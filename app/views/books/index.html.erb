<p id="notice"><%= notice %></p>

<h1>ようこそ<strong> Bookers </strong>へ！</h1>

<table>
  <thead>
    <tr>
      <th><strong>Bookers</strong>では、さまざまな書籍に関するあなたの意見や</th>
      <th>印象を共有し交換することができます</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.title %></td>
        <td><%= book.body %></td>
        <td><%= link_to 'Show', book %></td>
        <td><%= link_to 'Edit', edit_book_path(book) %></td>
        <td><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'start', new_book_path %>
